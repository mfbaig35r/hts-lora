# Training config for 8x V100 16GB (Lambda Cloud)
# Effective batch size: 2 * 8 GPUs * 2 grad_accum = 32

model:
  model_id: "nvidia/Llama-3.1-Nemotron-Nano-8B-v1"
  torch_dtype: "float16"
  attn_implementation: "eager"  # V100 doesn't support Flash Attention 2

quantization:
  load_in_4bit: true
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_compute_dtype: "float16"
  bnb_4bit_use_double_quant: true

lora:
  r: 32
  lora_alpha: 64
  lora_dropout: 0.05
  target_modules:
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"
  bias: "none"
  task_type: "CAUSAL_LM"

training:
  num_train_epochs: 3
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 4
  learning_rate: 2.0e-4
  weight_decay: 0.01
  warmup_ratio: 0.05
  lr_scheduler_type: "cosine"
  max_seq_length: 1536
  logging_steps: 10
  eval_steps: 1000
  save_steps: 1000
  seed: 42
  bf16: false
  fp16: true
  gradient_checkpointing: true
  optim: "paged_adamw_8bit"

data_dir: "data/formatted"
output_dir: "outputs"
